<view  wx:for="{{items}}">
  <view class="title">{{item.title}}</view>
  <button wx:for="{{item.apis}}" wx:for-item="apiItem" bindtap="bindTap" data-url="{{apiItem.url}}">
      {{apiItem.apititle}}
  </button>
</view>