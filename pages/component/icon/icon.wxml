<view>
  <block wx:for="{{iconSize}}">
   <icon type="success" size="{{item}}"></icon>
  </block>
</view>

<view>
  <block wx:for="{{iconColor}}">
    <icon type="waiting" color="{{item}}" size="35"></icon>
  </block>
</view>

<view>
  <block wx:for="{{iconType}}">
    <icon type="{{item}}"></icon>
  </block>
</view>
